# TheFarmerWasReplaced
農家は Replace() されました

[Steam：農家は Replace() されました](https://store.steampowered.com/app/2060160/_Replace/) のサンプルプログラムです。

### 使い方

各フォルダに格納されたファイルの中身を `module.py` と同じ階層に作成したファイル（`main.py`, `f0.py` など）にコピーして実行してください。
